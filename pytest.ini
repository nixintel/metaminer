[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
markers =
    unit: no infrastructure required — runs anywhere
    integration: requires a running Postgres instance (TEST_DATABASE_URL)
